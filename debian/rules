#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ --with gir

override_dh_makeshlibs:
	dh_makeshlibs --exclude plugin

override_dh_missing:
	dh_missing --fail-missing

override_dh_fixperms:
	dh_fixperms --exclude .js
